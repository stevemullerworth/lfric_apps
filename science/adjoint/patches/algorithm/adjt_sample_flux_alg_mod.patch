@@ -10,7 +10,7 @@
     use function_space_collection_mod, only : function_space_collection
     use sample_flux_kernel_mod, only : sample_flux_kernel_type
     use adj_sample_flux_kernel_mod, only : adj_sample_flux_kernel_type
-    use finite_element_config_mod, only : element_order
+    use finite_element_config_mod, only : element_order_h,element_order_v
     use fs_continuity_mod, only : w0, w2
     use constants_mod, only : i_def, r_def
     use setop_random_kernel_mod, only : setop_random_kernel_type
@@ -42,8 +42,8 @@
     real(kind=r_def) :: MachineTol
     real(kind=r_def) :: relative_diff
 
-    vector_space_w0_ptr => function_space_collection%get_fs(mesh,element_order,w0)
-    vector_space_w2_ptr => function_space_collection%get_fs(mesh,element_order,w2)
+    vector_space_w0_ptr => function_space_collection%get_fs(mesh,element_order_h,element_order_v,w0)
+    vector_space_w2_ptr => function_space_collection%get_fs(mesh,element_order_h,element_order_v,w2)
     call flux%initialise(vector_space=vector_space_w2_ptr, name='flux')
     call u%initialise(vector_space=vector_space_w2_ptr, name='u')
     call rmultiplicity%initialise(vector_space=vector_space_w2_ptr, name='rmultiplicity')
